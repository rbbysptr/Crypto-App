import React from "react";
import { RouterProvider } from "react-router-dom";

function App({ router }) {
  return (
    <RouterProvider router={router}>
    </RouterProvider>
  );
}

export default App;
